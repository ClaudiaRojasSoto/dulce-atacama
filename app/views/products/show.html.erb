<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-6">
    <%= link_to "← Volver a productos", products_path, class: "text-pink-600 hover:text-pink-700 font-medium" %>
  </div>

  <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
      <div class="p-8">
        <% if @product.images.attached? %>
          <div class="mb-4">
            <%= image_tag @product.images.first, class: "w-full h-96 object-cover rounded-xl shadow-lg" %>
          </div>
          
          <% if @product.images.count > 1 %>
            <div class="grid grid-cols-4 gap-2">
              <% @product.images[1..].each do |image| %>
                <%= image_tag image, class: "w-full h-20 object-cover rounded-lg hover:opacity-75 transition cursor-pointer" %>
              <% end %>
            </div>
          <% end %>
        <% else %>
          <div class="w-full h-96 bg-gradient-to-br from-pink-200 to-purple-200 rounded-xl flex items-center justify-center">
            <span class="text-9xl">🍰</span>
          </div>
        <% end %>
      </div>

      <div class="p-8 flex flex-col justify-center">
        <div class="mb-4">
          <span class="inline-block bg-purple-100 text-purple-800 px-3 py-1 rounded-full text-sm font-medium">
            <%= @product.category.name %>
          </span>
        </div>
        
        <h1 class="text-4xl font-bold text-gray-800 mb-4"><%= @product.name %></h1>
        
        <div class="mb-6">
          <span class="text-5xl font-bold text-pink-600">$<%= number_with_delimiter(@product.price) %></span>
        </div>
        
        <div class="mb-8">
          <h2 class="text-xl font-semibold text-gray-800 mb-2">Descripción</h2>
          <p class="text-gray-600 leading-relaxed"><%= @product.description %></p>
        </div>
        
        <div class="bg-gradient-to-r from-pink-50 to-purple-50 rounded-xl p-6 mb-6">
          <h3 class="font-semibold text-gray-800 mb-3">Información importante:</h3>
          <ul class="space-y-2 text-gray-700">
            <li class="flex items-start">
              <span class="mr-2">⏰</span>
              <span>Pedidos con 24 horas de anticipación</span>
            </li>
            <li class="flex items-start">
              <span class="mr-2">🚗</span>
              <span>Servicio de delivery en Copiapó</span>
            </li>
            <li class="flex items-start">
              <span class="mr-2">📞</span>
              <span>Te contactaremos por WhatsApp</span>
            </li>
          </ul>
        </div>
        
        <% if user_signed_in? %>
          <%= link_to "Hacer Pedido", new_order_path(product_id: @product.id), class: "w-full block text-center bg-gradient-to-r from-pink-500 to-purple-600 text-white py-4 rounded-xl text-lg font-semibold hover:from-pink-600 hover:to-purple-700 transition shadow-lg hover:shadow-xl transform hover:-translate-y-1" %>
        <% else %>
          <div class="text-center">
            <p class="text-gray-600 mb-4">Inicia sesión para hacer un pedido</p>
            <%= link_to "Iniciar Sesión", new_user_session_path, class: "inline-block bg-gradient-to-r from-pink-500 to-purple-600 text-white px-8 py-4 rounded-xl text-lg font-semibold hover:from-pink-600 hover:to-purple-700 transition shadow-lg" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
