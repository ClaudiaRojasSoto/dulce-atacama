<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-8">
    <%= link_to "← Volver a mis pedidos", orders_path, class: "text-pink-600 hover:text-pink-700 font-medium" %>
  </div>

  <div class="bg-white rounded-xl shadow-lg overflow-hidden">
    <div class="bg-gradient-to-r from-pink-500 to-purple-600 text-white px-8 py-6">
      <div class="flex justify-between items-center">
        <div>
          <h1 class="text-3xl font-bold mb-2">Pedido #<%= @order.id %></h1>
          <p class="text-pink-100">Creado el <%= @order.created_at.strftime('%d/%m/%Y %H:%M') %></p>
        </div>
        <div class="text-right">
          <span class="inline-block px-4 py-2 bg-white text-purple-600 rounded-lg font-bold text-lg">
            <%= @order.status.titleize %>
          </span>
        </div>
      </div>
    </div>

    <div class="p-8">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
        <div>
          <h2 class="text-xl font-bold text-gray-800 mb-4">Información de Entrega</h2>
          <div class="space-y-3">
            <div>
              <span class="text-sm text-gray-600 block">Teléfono:</span>
              <span class="text-gray-900 font-medium"><%= @order.phone %></span>
            </div>
            <div>
              <span class="text-sm text-gray-600 block">Dirección:</span>
              <span class="text-gray-900 font-medium"><%= @order.delivery_address %></span>
            </div>
            <div>
              <span class="text-sm text-gray-600 block">Fecha de Entrega:</span>
              <span class="text-gray-900 font-medium"><%= @order.delivery_date.strftime('%d/%m/%Y %H:%M') %></span>
            </div>
          </div>
        </div>

        <div>
          <h2 class="text-xl font-bold text-gray-800 mb-4">Resumen del Pedido</h2>
          <div class="bg-gradient-to-r from-pink-50 to-purple-50 rounded-lg p-4">
            <div class="text-sm text-gray-600 mb-1">Total a Pagar:</div>
            <div class="text-4xl font-bold text-pink-600">$<%= number_with_delimiter(@order.total) %></div>
          </div>
        </div>
      </div>

      <div class="mb-8">
        <h2 class="text-xl font-bold text-gray-800 mb-4">Productos</h2>
        <div class="space-y-4">
          <% @order.order_items.each do |item| %>
            <div class="flex items-center justify-between p-4 bg-gray-50 rounded-lg">
              <div class="flex items-center space-x-4">
                <% if item.product.images.attached? %>
                  <%= image_tag item.product.images.first, class: "h-16 w-16 rounded-lg object-cover" %>
                <% else %>
                  <div class="h-16 w-16 bg-gradient-to-br from-pink-200 to-purple-200 rounded-lg flex items-center justify-center text-2xl">🍰</div>
                <% end %>
                
                <div>
                  <h3 class="font-bold text-gray-800"><%= item.product.name %></h3>
                  <p class="text-sm text-gray-600">Cantidad: <%= item.quantity %> × $<%= number_with_delimiter(item.price) %></p>
                </div>
              </div>
              
              <div class="text-right">
                <span class="text-lg font-bold text-gray-800">$<%= number_with_delimiter(item.subtotal) %></span>
              </div>
            </div>
          <% end %>
        </div>
      </div>

      <% if @order.notes.present? %>
        <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 rounded-r-lg">
          <h3 class="font-semibold text-gray-800 mb-2">Notas adicionales:</h3>
          <p class="text-gray-700"><%= @order.notes %></p>
        </div>
      <% end %>
    </div>
  </div>

  <div class="mt-6 bg-gradient-to-r from-green-50 to-blue-50 border-l-4 border-green-400 p-6 rounded-r-lg">
    <h3 class="font-semibold text-gray-800 mb-2">📱 ¿Necesitas hacer cambios?</h3>
    <p class="text-gray-700 mb-3">Contáctanos por WhatsApp para cualquier modificación o consulta sobre tu pedido.</p>
    <a href="https://wa.me/56999999999?text=<%= ERB::Util.url_encode("Hola, consulta sobre el pedido ##{@order.id}") %>" 
       target="_blank" 
       class="inline-block bg-green-500 text-white px-6 py-3 rounded-lg hover:bg-green-600 transition font-semibold">
      💬 Contactar por WhatsApp
    </a>
  </div>
</div>
