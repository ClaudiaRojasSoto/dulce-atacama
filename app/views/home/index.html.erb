<div class="relative overflow-hidden">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <div class="text-center mb-16">
      <h1 class="text-5xl md:text-6xl font-bold mb-6 bg-gradient-to-r from-pink-600 to-purple-600 bg-clip-text text-transparent">
        Bienvenido a Dulce Atacama
      </h1>
      <p class="text-xl text-gray-600 mb-4">
        Pastelería artesanal en Copiapó, Región de Atacama
      </p>
      <p class="text-lg text-gray-500 mb-8">
        🚗 Servicio de delivery | ⏰ Pedidos con 24 horas de anticipación
      </p>
      <%= link_to "Ver Productos", products_path, class: "inline-block bg-gradient-to-r from-pink-500 to-purple-600 text-white px-8 py-4 rounded-full text-lg font-semibold hover:from-pink-600 hover:to-purple-700 transition shadow-lg hover:shadow-xl transform hover:-translate-y-1" %>
    </div>

    <% if @active_promotions.any? %>
      <div class="mb-16">
        <h2 class="text-3xl font-bold text-center mb-8 text-gray-800">🎉 Promociones Activas</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <% @active_promotions.each do |promotion| %>
            <div class="bg-gradient-to-r from-yellow-50 to-orange-50 border-2 border-orange-300 rounded-xl p-6 shadow-lg">
              <h3 class="text-2xl font-bold text-orange-600 mb-2"><%= promotion.title %></h3>
              <p class="text-gray-700 mb-3"><%= promotion.description %></p>
              <div class="flex items-center justify-between">
                <span class="text-3xl font-bold text-orange-600"><%= promotion.discount_percentage %>% OFF</span>
                <span class="text-sm text-gray-600">
                  Válido hasta <%= promotion.valid_until.strftime('%d/%m/%Y') %>
                </span>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>

    <div class="mb-16">
      <h2 class="text-3xl font-bold text-center mb-8 text-gray-800">Productos Destacados</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <% @featured_products.each do |product| %>
          <div class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-2xl transition transform hover:-translate-y-2">
            <% if product.images.attached? %>
              <%= image_tag product.images.first, class: "w-full h-64 object-cover" %>
            <% else %>
              <div class="w-full h-64 bg-gradient-to-br from-pink-200 to-purple-200 flex items-center justify-center">
                <span class="text-6xl">🍰</span>
              </div>
            <% end %>
            
            <div class="p-6">
              <h3 class="text-xl font-bold text-gray-800 mb-2"><%= product.name %></h3>
              <p class="text-sm text-gray-600 mb-4 line-clamp-2"><%= product.description %></p>
              
              <div class="flex items-center justify-between">
                <span class="text-2xl font-bold text-pink-600">$<%= number_with_delimiter(product.price) %></span>
                <%= link_to "Ver Detalles", product_path(product), class: "bg-gradient-to-r from-pink-500 to-purple-600 text-white px-4 py-2 rounded-lg hover:from-pink-600 hover:to-purple-700 transition font-medium shadow-md" %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <div class="bg-gradient-to-r from-pink-100 to-purple-100 rounded-2xl p-8 text-center shadow-xl">
      <h2 class="text-3xl font-bold mb-4 text-gray-800">¿Cómo hacer un pedido?</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mt-8">
        <div class="bg-white rounded-xl p-6 shadow-md">
          <div class="text-4xl mb-3">📱</div>
          <h3 class="font-bold text-lg mb-2">1. Elige tus productos</h3>
          <p class="text-gray-600">Navega por nuestro catálogo y selecciona tus favoritos</p>
        </div>
        
        <div class="bg-white rounded-xl p-6 shadow-md">
          <div class="text-4xl mb-3">📅</div>
          <h3 class="font-bold text-lg mb-2">2. Programa tu entrega</h3>
          <p class="text-gray-600">Selecciona fecha y hora con al menos 24 horas de anticipación</p>
        </div>
        
        <div class="bg-white rounded-xl p-6 shadow-md">
          <div class="text-4xl mb-3">🚗</div>
          <h3 class="font-bold text-lg mb-2">3. Recibe en tu domicilio</h3>
          <p class="text-gray-600">Te contactaremos por WhatsApp y entregaremos tu pedido</p>
        </div>
      </div>
    </div>
  </div>
</div>
